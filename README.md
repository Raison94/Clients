A crud operation for mongo vue express node js full stack application.

You should install the dependencies for clients branch run npm install --save, go also to the path of the folder client and run the same command in cmd. The port for backend starts at 3000 while the port for the frontend starts at 8080.
